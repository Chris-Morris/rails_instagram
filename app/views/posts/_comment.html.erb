<% if comment.created_at? %>
    <div class="well">
        <p><strong><%= current_user.email %></strong>: <%= comment.body %></p>
        <p style="font-size: 12px; color: grey;"><%= time_ago_in_words(comment.created_at) %></p>
    </div>
    <% else %>
    <div class="well">
        <p><strong><%= current_user.email %></strong>: <%= comment.body %></p>
        <% print comment.user %>
    </div>
<% end %>